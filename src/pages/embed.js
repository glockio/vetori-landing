// Vetori Embed — landing page entry.
//
// Loads the shared brand foundation, the (currently empty) page-specific
// styles, and the shared interaction behaviors. The embed page mostly
// uses brand utilities; page-specific bits land in embed.css if/when they
// emerge.

import '../brand.css'
import './embed.css'
import '../shared/interactions.js'
