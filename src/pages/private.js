// Vetori Private — landing page entry.
//
// Loads the shared brand foundation, the page-specific styles, and the
// shared interaction behaviors (CTA handlers, scroll-reveal, smooth
// anchor scroll, closed-beta toggle).
//
// All authoring-app URLs come from src/config.js — never hardcoded.

import '../brand.css'
import './private.css'
import '../shared/interactions.js'
