# Authoring app URL — region-prefixed. Override per deploy environment.
VITE_PUBLIC_APP_URL=https://app.us.vetori.ai

# When true, the page shows "closed beta" copy + "Join the waitlist"
# instead of the open-signup CTA.
VITE_CLOSED_REGISTRATION=true
